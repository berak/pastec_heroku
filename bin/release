#!/bin/sh
# bin/release <build-dir>

echo "--- {}"
#!/bin/sh

cat << EOF
---
addons:
default_process_types:
  web: ./pastec_server -p $PORT visualWordsORB.dat
config_vars:
PATH: /app/bin:/usr/local/bin:/usr/bin:/bin
EOF
